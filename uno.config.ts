// uno.config.ts
import { defineConfig } from "unocss";

export default defineConfig({
  theme: {
    fontFamily: {
      inter: "Inter",
      gabarito: "Gabarito",
    },
    colors: {
      dark: "hsl(0, 0%, 10%)",
    },
  },
});
