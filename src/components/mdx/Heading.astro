---
export type Props = {
  as?: "h1" | "h2" | "h3";
  content: string;
};
const { as = "h1", content, ...props } = Astro.props;

const Element = as;
---

<Element {...props} class="prose mx-a mb-4 mt-8 underline">
  {content}
</Element>
